import 'package:flutter/material.dart';

class createFoot extends StatelessWidget{
  const createFoot({Key? key}):super(key:key);

  Widget build(BuildContext context){
    return Scaffold(
      body:Center(
        child: Text('글쓰기')
        ),
    );
  }

}

